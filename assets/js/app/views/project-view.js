// Generated by CoffeeScript 1.6.2
(function() {
  define([], function() {
    var ProjectView;

    ProjectView = Backbone.View.extend({
      initialize: function() {
        var $content;

        _.bindAll(this);
        this.$el = $(this.el);
        this.$el.append("<table cellpadding=\"0\" cellspacing=\"0\" cellpadding=\"0\"><tr></tr></table>");
        _.each(this.$('img'), this.fixImage);
        $content = this.$('.content');
        $content.wrap("<td class=\"description\"></td>");
        $content = $content.parents('.description');
        return $(this.$('.image')[0]).after($content);
      },
      fixImage: function(el) {
        var $el;

        $el = $(el);
        $el.wrap("<td class=\"image\"></td>");
        $el = $el.parents('.image');
        return this.$('tr').append($el);
      }
    });
    return ProjectView;
  });

}).call(this);
